import React from 'react';
import { Block } from './Block';
import { Icon } from './icons/Icon';

interface BtnProps {
	icon: {
		name;
	};
}

export const Btn: React.FC<BtnProps> = ({ children, icon }) => {
	return <Block></Block>;
};
